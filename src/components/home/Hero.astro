---
import { Image } from 'astro:assets';
import avatar from '@site/assets/avatar.png';
---

<section class="col-span-5 flex flex-col justify-start gap-2 md:pe-12">
    <div class="flex flex-col md:flex-row items-center md:items-center gap-2">
        <span class="text-4xl/10 font-header text-outline font-medium text-center md:mt-8 w-full md:w-auto">Kamusta!</span>
        <div class="flex items-center gap-2">
            <span class="text-4xl/10 font-header text-outline font-medium md:mt-8">I'm</span>
            <Image src={avatar} alt="John" class="h-32 w-32 inline-block -mx-2" />
            <span id="name" class="text-4xl/10 font-header md:mt-8 font-bold">John!</span>
        </div>
    </div>
    <span class="text-3xl/10 font-header font-medium text-outline text-center md:text-start">
        <span>I'm a</span>
        <span class="font-semibold text-tertiary">junior software dev</span>
        <span>based in</span>
        <span class="font-semibold text-secondary">Cavite, Philippines.</span>
    </span>
    <span class="text-outline-variant">
        Software developer, UX designer, and full-time nerd.
    </span>
</section>

<style>
    #name {
        background: linear-gradient(
        to bottom,
        #F2C864 0%,
        #F0BE67 25%,
        #E8AA65 50%,
        #F1A773 75%,
        #F09F72 100%
        );
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        color: #F2C864; /* Fallback color */
    }
</style>